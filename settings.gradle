pluginManagement {
    plugins {
        id 'com.android.library' version '8.1.4'
    }
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "channels-sdk"
